services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: linkquote
      POSTGRES_USER: linkquote_user
      POSTGRES_PASSWORD: senha_local_123
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build: ./backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - db

volumes:
  postgres_data: